<div class="container" style="margin: 100px auto">
    <div class="row">
        <div class="col-6 offset-3">
            <h4>Para modificar una nueva promoción, complete la totalidad de los campos</h4>
            <input type="hidden" value="{{promocion.id}}" name="id">
            <form method="post" action="/admin/promociones/modificar">
                <div class="form-group">
                    <label for="">Titulo</label>
                    <input type="text" class="form-control" name="titulo" value="{{promocion.titulo}}">
                </div>
                <br>
                <div class="form-group">
                    <label for="">Cuerpo</label>
                    <input type="text" class="form-control" name="cuerpo" value="{{promocion.cuerpo}}">
                </div>
                <br>
                <div class="form-group">
                    <label for="">Vigencia</label>
                    <textarea name="vigencia" class="form-control">{{promocion.vigencia}}</textarea>
                </div>
                <br>     
                <button type="submit" class="btn btn-primary">Modificar</button>
            </form>
            {{#if error}}
                <h5>{{message}}</h5>
            {{/if}}
        </div>
    </div>
</div>
